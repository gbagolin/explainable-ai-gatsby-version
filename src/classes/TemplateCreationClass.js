/**
 * Class used to keep track of the rules of all actions
 */
export default class TemplateCreationClass {
  /**
   *
   * @param {*} rules List of Rule object, for each action one rule object
   */
  constructor(rules) {
    this.rules = rules
  }
}
