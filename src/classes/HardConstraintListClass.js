/**
 * Class used to keep track of all the hardConstraints present.
 */
export default class HardConstraintListClass {
    /**
     * 
     * @param {*} hardConstraintList 
     */
  constructor(hardConstraintList) {
    this.hardConstraintList = hardConstraintList
  }
}
