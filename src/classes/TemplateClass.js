/**
 * Class used to keep track of the various actions used in the rule
 */
export default class TemplateClass {
  /**
   *
   * @param {*} id
   * @param {*} name
   */
  constructor(id, name) {
    this.id = id
    this.name = name
  }
}
